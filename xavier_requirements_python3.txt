# Xavier Python 3 Environment Requirements
# Compatible with NVIDIA Jetson Xavier AGX 32G with CUDA support
# Updated for Python 3.8+ environment

# Core Python packages for Xavier ARM64
torch>=1.12.0  # Xavier-compatible PyTorch with CUDA support
torchvision>=0.13.0  # Matching torchvision version
numpy>=1.21.0  # ARM64 optimized numpy
psutil>=5.8.0  # System monitoring

# Scientific computing (ARM64 compatible)
scipy>=1.7.0  # Optimized for ARM64
scikit-learn>=1.0.0  # Machine learning utilities

# Data handling
pandas>=1.3.0  # Data manipulation
h5py>=3.6.0  # HDF5 file format support

# Visualization (optional but useful for analysis)
matplotlib>=3.5.0  # Plotting library
seaborn>=0.11.0  # Statistical visualization

# System monitoring and hardware info
py-cpuinfo>=8.0.0  # CPU information
GPUtil>=1.4.0  # GPU monitoring

# JSON and configuration
json5>=0.9.0  # Enhanced JSON support for configs

# Xavier-specific optimizations (install manually if needed)
# These require manual installation from NVIDIA sources:
# tensorrt>=8.0.0  # TensorRT acceleration (JetPack included)
# pycuda>=2021.1   # Direct CUDA programming interface

# Development and testing
pytest>=6.2.0  # Testing framework
tqdm>=4.62.0  # Progress bars

# Memory profiling (useful for edge device optimization)
memory-profiler>=0.60.0  # Memory usage monitoring
line-profiler>=4.0.0  # Line-by-line profiling

# System utilities
python-dateutil>=2.8.0  # Date/time utilities
pathlib2>=2.3.0  # Path handling (for older compatibility if needed)

# Note: Install order recommendations for Xavier:
# 1. First install PyTorch from NVIDIA wheel
# 2. Then install torchvision from source
# 3. Finally install remaining packages with pip