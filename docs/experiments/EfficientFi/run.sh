#!/usr/bin/env bash
set -euo pipefail
source .venv/bin/activate || true
# Placeholder: clone official repo and run baseline script
# git clone <official_repo_url> src && cd src
# bash scripts/train.sh --config configs/<cfg>.yaml --output ../results
# Collect metrics to results/metrics.json
